{
  "name": "iteradora-lambda",
  "version": "2.0.0",
  "description": "Lambda backend IteraDORA - AWS Bedrock (Claude) + Ollama fallback",
  "main": "index.js",
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.700.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
